import type { ReactNode } from 'react';
export interface StepItem {
    title: ReactNode;
    description?: ReactNode;
    /** Override automatic status derived from `current` */
    status?: 'wait' | 'process' | 'finish' | 'error';
    /** Custom icon for this step */
    icon?: ReactNode;
}
interface StepsProps {
    /** Ordered list of step definitions */
    items: StepItem[];
    /** Zero-based index of the active step */
    current?: number;
    /** Layout direction (default: 'horizontal') */
    direction?: 'horizontal' | 'vertical';
    /** Size preset (default: 'md') */
    size?: 'sm' | 'md';
    className?: string;
    /** Called when a finished step is clicked */
    onChange?: (index: number) => void;
}
export declare function Steps({ items, current, direction, size, className, onChange, }: StepsProps): import("react/jsx-runtime").JSX.Element;
export {};
//# sourceMappingURL=steps.d.ts.map