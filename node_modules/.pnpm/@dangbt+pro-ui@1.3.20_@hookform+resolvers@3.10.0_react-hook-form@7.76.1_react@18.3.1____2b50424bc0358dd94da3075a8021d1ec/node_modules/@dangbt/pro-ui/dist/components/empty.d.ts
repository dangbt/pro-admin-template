import type { ReactNode } from 'react';
interface EmptyProps {
    /** Custom icon or image. Defaults to an inbox icon. */
    image?: ReactNode;
    /** Primary text. Defaults to "No data". */
    description?: ReactNode;
    /** Extra content below description (e.g. an action button) */
    children?: ReactNode;
    className?: string;
}
export declare function Empty({ image, description, children, className }: EmptyProps): import("react/jsx-runtime").JSX.Element;
export {};
//# sourceMappingURL=empty.d.ts.map