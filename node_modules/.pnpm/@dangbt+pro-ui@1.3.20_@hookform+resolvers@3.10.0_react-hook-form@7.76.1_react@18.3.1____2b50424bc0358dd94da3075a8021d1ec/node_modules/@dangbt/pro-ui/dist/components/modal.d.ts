import React from 'react';
import { type ModalOverlayProps } from 'react-aria-components';
type ModalSize = 'sm' | 'md' | 'lg' | 'xl' | 'full';
interface ModalProps extends Omit<ModalOverlayProps, 'className' | 'children'> {
    title?: string;
    children: React.ReactNode | ((props: {
        close: () => void;
    }) => React.ReactNode);
    footer?: React.ReactNode | ((props: {
        close: () => void;
    }) => React.ReactNode);
    size?: ModalSize;
    className?: string;
    /**
     * Optional trigger element. When provided the modal manages its own open
     * state via DialogTrigger and pressed:scale-95 on the trigger is neutralised
     * (React Aria keeps data-pressed=true while the overlay is open).
     * When omitted, use isOpen / onOpenChange for controlled mode.
     */
    triggerElement?: React.ReactNode;
}
export declare function Modal({ triggerElement, ...props }: ModalProps): import("react/jsx-runtime").JSX.Element;
interface ConfirmModalProps extends Omit<ModalProps, 'children' | 'footer' | 'size'> {
    description?: string;
    confirmLabel?: string;
    cancelLabel?: string;
    onConfirm?: () => void;
    danger?: boolean;
}
export declare function ConfirmModal({ description, confirmLabel, cancelLabel, onConfirm, danger, ...props }: ConfirmModalProps): import("react/jsx-runtime").JSX.Element;
export {};
//# sourceMappingURL=modal.d.ts.map