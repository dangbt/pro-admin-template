import { type ReactNode } from 'react';
import { cn } from '../../lib/cn';
import type { Column } from '@tanstack/react-table';
interface ColumnToggleItem {
    id: string;
    label: string;
    canHide: boolean;
    isVisible: boolean;
    toggle: () => void;
}
interface ToolbarProps {
    title?: string;
    actions?: ReactNode[];
    onRefresh?: () => void;
    columnToggles?: ColumnToggleItem[];
}
export declare function Toolbar({ title, actions, onRefresh, columnToggles }: ToolbarProps): import("react/jsx-runtime").JSX.Element | null;
export declare function buildColumnToggles<T>(columns: Column<T, unknown>[]): ColumnToggleItem[];
export { cn };
//# sourceMappingURL=toolbar.d.ts.map