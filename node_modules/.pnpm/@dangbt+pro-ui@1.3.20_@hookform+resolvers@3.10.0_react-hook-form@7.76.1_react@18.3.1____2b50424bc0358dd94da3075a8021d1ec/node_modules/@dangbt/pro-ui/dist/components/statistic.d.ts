import type { ReactNode } from 'react';
interface StatisticProps {
    /** Primary metric value */
    value: ReactNode;
    /** Label shown above the value */
    title: ReactNode;
    /** Optional prefix element (icon, currency symbol…) */
    prefix?: ReactNode;
    /** Optional suffix element (unit, %, …) */
    suffix?: ReactNode;
    /** Small trend badge rendered next to the value */
    trend?: {
        value: ReactNode;
        direction?: 'up' | 'down' | 'neutral';
    };
    /** Additional CSS class for the root element */
    className?: string;
    /** Format a numeric value — applied when `value` is a number */
    formatter?: (val: number) => ReactNode;
    /** Size preset */
    size?: 'sm' | 'md' | 'lg';
}
export declare function Statistic({ value, title, prefix, suffix, trend, className, formatter, size, }: StatisticProps): import("react/jsx-runtime").JSX.Element;
export {};
//# sourceMappingURL=statistic.d.ts.map