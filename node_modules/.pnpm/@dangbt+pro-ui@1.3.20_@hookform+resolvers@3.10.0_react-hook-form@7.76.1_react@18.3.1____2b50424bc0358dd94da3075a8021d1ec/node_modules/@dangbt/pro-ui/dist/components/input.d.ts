import { type TextFieldProps } from 'react-aria-components';
import type { ReactNode } from 'react';
import { type Size } from '../lib/size';
interface InputProps extends Omit<TextFieldProps, 'className'> {
    label?: string;
    description?: string;
    placeholder?: string;
    type?: 'text' | 'email' | 'password' | 'url' | 'tel' | 'search' | 'datetime-local';
    size?: Size;
    className?: string;
    inputClassName?: string;
    errorMessage?: ReactNode;
}
export declare function Input({ label, description, placeholder, type, size, className, inputClassName, errorMessage, ...props }: InputProps): import("react/jsx-runtime").JSX.Element;
export {};
//# sourceMappingURL=input.d.ts.map