import { type Size } from '../lib/size';
export interface AsyncSelectOption {
    value: string;
    label: string;
}
export interface AsyncSelectFetchResult<T = AsyncSelectOption> {
    options: T[];
    hasMore: boolean;
}
interface AsyncSelectProps<T extends AsyncSelectOption = AsyncSelectOption> {
    label?: string;
    placeholder?: string;
    searchPlaceholder?: string;
    defaultLabel?: string;
    size?: Size;
    className?: string;
    value?: string | null;
    onChange?: (value: string | null, option: T | null) => void;
    fetchOptions: (params: {
        search: string;
        page: number;
        pageSize: number;
    }) => Promise<AsyncSelectFetchResult<T>>;
    pageSize?: number;
    debounceMs?: number;
    isDisabled?: boolean;
    isInvalid?: boolean;
    onBlur?: () => void;
}
export declare function AsyncSelect<T extends AsyncSelectOption = AsyncSelectOption>({ label, placeholder, searchPlaceholder, defaultLabel, size, className, value, onChange, fetchOptions, pageSize, debounceMs, isDisabled, isInvalid, onBlur, }: AsyncSelectProps<T>): import("react/jsx-runtime").JSX.Element;
export {};
//# sourceMappingURL=async-select.d.ts.map